from __future__ import annotations


class _Unset:
    """Sentinel for 'no value provided'.

    Use the class itself as the sentinel — not an instance — so it works
    as a default argument without module-level object() assignment.
    """
