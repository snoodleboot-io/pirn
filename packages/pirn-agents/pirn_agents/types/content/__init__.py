"""Multimodal content blocks that make up agent message payloads.

Frozen value types for the content of a message: the :class:`ContentBlock`
base and its variants (text, image, audio, file), the :class:`MediaHandle`
reference wrapper, the :class:`MessageContent` sequence, and the
:class:`ToolResultBlock` that renders a tool outcome as content.
"""

__all__: list[str] = []
