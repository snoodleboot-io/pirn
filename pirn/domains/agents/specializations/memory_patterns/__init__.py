"""Reusable agent memory pattern pipelines.

Each pipeline is a :class:`SubTapestry` that composes the agent core
layer (``MemoryStore``, ``LLMProvider``) into a named memory shape:
episodic, semantic, procedural, or working.
"""

__all__: list[str] = []
